# This package has moved

> **Consolidated into the [goldlapel-js monorepo](https://github.com/goldlapel/goldlapel-js).**

The source code now lives at `packages/prisma/` (or `packages/drizzle/`) in the goldlapel-js repo.

Install with:

```bash
npm install @goldlapel/prisma
# or
npm install @goldlapel/drizzle
```
